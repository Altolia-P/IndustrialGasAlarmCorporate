# 本地开发配置模板
# 复制此文件为 application-local.yml，填入你的本地密码
# application-local.yml 已加入 .gitignore，不会提交到 Git

spring:
  datasource:
    password: 你的数据库密码
  cloud:
    nacos:
      discovery:
        enabled: true
      config:
        enabled: false
