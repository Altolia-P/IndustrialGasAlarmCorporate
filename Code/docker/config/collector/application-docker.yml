# ============================================================
# Docker 环境专属配置 — Collector 模块
# ============================================================

spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
